# fullExample

This example documents the complete policy and observation configuration surface currently implemented by the controller.

Some alternatives are mutually exclusive and therefore shown as comments, notably `control.period_s` versus `control.frequency_hz`, scalar versus mapped action scaling, and named versus explicit joint selectors.
